# ADSENSE_READINESS_AND_ANALYTICS_REPORT

生成日: 2026-08-02
対象: オーストラリアワーホリコンパス（whv-guide-australia）
ブランチ: main（ローカルコミットのみ・push なし）
コミット: `feat: improve AdSense readiness and add GA4 analytics`

このレポートは、Google AdSense の「有用性の低いコンテンツ（低価値コンテンツ）」による
不承認を受けたサイトのリスク調査と、Google Analytics 4（GA4）実装、信頼ページ整備、
インデックス品質の是正結果をまとめたものです。

---

## 1. 確認した不承認メッセージ

- AdSense からの不承認理由: **`有用性の低いコンテンツ`**
- 付随ガイダンスが参照していた基準:
  - 最小コンテンツ要件
  - 独自性・高品質なコンテンツ
  - 良好なユーザー体験
  - 内容の薄い／低品質なコンテンツのガイドライン
  - ウェブマスター向け品質ガイドライン
- **正確な原因は AdSense からは特定されていない。**

## 2. 重要な限定事項（Important limitation）

- **Google/AdSense の正確な審査ロジック（判定基準の内部実装）は公開されておらず、入手できない。**
- 本レポートの指摘は、リポジトリ・マニフェスト・実公開ルートを根拠とした
  **サイト側のリスク評価**であり、**不承認理由を保証・断定するものではない。**
- したがって、修正は「観測可能なリスクの低減」を目的としており、承認を保証しない。

## 3. 修正前の公開ページ数

- 管理対象コンテンツ: **385 件**（既存 47 ＋ 新規公開 331 ＋ 統合 merged 7）。
- 実際に公開される記事 URL（merged はリダイレクトのため除外）: **378 件**。
- ビルド生成ルート（`next build`）: **440 ルート**（記事・カテゴリ・ハブ・ツール・
  ニュース・掲示板・信頼ページ・システムページを含む静的/動的プリレンダー）。
- インデックス対象（sitemap 掲載）には内部検索ページ `/search` が**含まれていた**。

## 4. 修正後の公開ページ数

- 管理対象コンテンツ: **385 件（変更なし。新規記事の量産は行っていない）**。
- 実公開記事 URL: **378 件（変更なし）**。
- ビルド生成ルート: **440 ルート**（`/editorial-policy` を新規追加。`✓ Compiled successfully`・
  440/440 静的ページ生成）。
- インデックス方針の変更（正味）:
  - **インデックスから除外: `/search`（1 ページ）** — `robots: noindex` 付与＋ sitemap から削除。
  - **インデックスに追加: `/editorial-policy`（1 ページ・信頼ページ）**。

## 5. 検出した主な低価値コンテンツリスク（分類付き）

| 重大度 | リスク | 状況 |
| --- | --- | --- |
| **High** | 内部検索結果ページ `/search` がインデックス可能（薄い・準重複 URL を生みやすい） | **修正済**（noindex＋sitemap 除去） |
| **High** | 編集方針／情報の作り方を説明する信頼ページが存在せず、E-E-A-T・透明性が弱い | **修正済**（`/editorial-policy` 新設） |
| **Medium** | プライバシーポリシーが未配信の広告（AdSense）をあたかも配信中のように記載し、実装（GA）と不一致 | **修正済**（実態に合わせて GA 開示・広告は未配信と明記） |
| **Medium** | 一部の可変・YMYL 情報が公式ソースの言い換えに留まりうる | 既存記事は `verifiedAt`＋公式ソース＋非断定で緩和済み（**手動レビュー推奨**） |
| **Low** | 掲示板（UGC）の投稿詳細が将来的に薄くなり得る | 現状は公開フィルタ・カテゴリ説明ありで許容（**継続監視**） |

## 6. 検出した薄い／汎用ページ

- **内部検索ページ `/search`**（High）: サーバー描画部分が薄く、クエリごとに準重複 URL を
  生成し得るため、AdSense の低価値コンテンツ観点で**インデックス除外**が妥当。→ 修正済。
- 記事本体・カテゴリ・ハブ・ツール・ダウンロード・ニュースは、直近の whole-site 監査で
  プレースホルダー／空ページ 0 と確認済み（本タスクで新たな薄いページの量産は行っていない）。
- `AdBanner` は環境変数 `NEXT_PUBLIC_ADSENSE_SLOT_BANNER` 未設定時に `null` を返すため、
  **可視の空広告プレースホルダーは存在しない**（低価値シグナルなし）。

## 7. 重複／内容が重なるページ

- 重複 slug / path / export: **0 件**（`validate:articles`）。
- 直近監査で真に重複と判断した狭いケースは **planned（未公開）段階で統合済**（308 恒久
  リダイレクト 7 件）。公開中 URL を潰す統合は発生していない。
- `validate:content` の 66 警告は**粗い intent ラベルによる過検出（false positive）**で、
  地名・州・作物など独立した検索意図を持つため統合対象ではない（dangling リンク 0）。

## 8. テンプレート由来のリスク

- ニューステンプレート（`/news/templates/*`・6 件）は**利用者が記入する雛形**として明示され、
  空欄・必須項目・公式確認導線を持つ構造。機械的な量産記事ではない。
- 記事群は同一構造の反復ではあるが、各記事は地域・州・作物・シーンごとに異なる実務情報を持つ。
  本タスクでは**強い記事の不要な書き換えは行わない**方針を維持（品質 > 公開記事総数）。
- **手動レビュー推奨（Manual）**: 「導入・結論・FAQ の言い回しが近い」箇所の抜き取り確認。

## 9. 改善したページ

- **プライバシーポリシー `/privacy`**: GA4 の実際の利用を開示、広告は未配信と明記、
  同意管理の将来対応を追記、最終更新日を更新。
- **運営者情報 `/about`**: 編集方針ページへの導線を追加（信頼性の相互リンク強化）。
- **フッター**: 全ページから「編集方針」への導線を追加。
- （記事本文の事実内容は未検証の一括改変を避けるため変更していない。）

## 10. 非公開化・除外・noindex にしたページ

- **`/search`（内部検索結果ページ）**: `robots: { index: false, follow: true }` を付与し、
  sitemap から削除。クロールは可能だがインデックスされない（重要コンテンツのクロールは
  阻害しない）。**削除・リダイレクトは行っていない**（ページは機能として存続）。

## 11. 作成・更新した信頼ページ

| ページ | 状況 |
| --- | --- |
| 運営者情報 `/about` | 既存（運営者 Nammoku Solutions・免責・著作権・問い合わせ導線）＋編集方針リンク追加 |
| お問い合わせ／修正受付 `/request` | 既存（フィードバックフォーム・記事リクエスト・修正依頼・体験談共有） |
| プライバシーポリシー `/privacy` | **更新**（GA 開示・広告未配信明記・同意管理の将来対応・更新日） |
| 利用規約 `/terms` | 既存（免責・禁止事項） |
| **編集方針・情報の作り方 `/editorial-policy`** | **新規作成** |
| 免責事項 | `/about`・`/terms` に既存（別ページ化は不要と判断） |

`/editorial-policy` が説明する内容: トピックの選び方／公式情報の確認方法／確認日
（`verifiedAt`）の扱い／州ごとの違いの扱い／体験ベース情報と公式情報の区別／
修正・フィードバックの受付／AI を活用した制作の確認。
**専門家（弁護士・移民/ビザ・税理士・会計士・医療従事者）による個別確認を受けていると
称する表現は用いていない**（事実として確立していないため）。

## 12. 修正したナビゲーション／UX の問題

- フッターに「編集方針」導線を追加（信頼ページへの到達性向上）。
- `/about` → `/editorial-policy` の相互リンクを追加。
- 内部検索ページのインデックス除外により、検索エンジンに露出する薄い URL を削減。
- 既存のヘッダー／フッター／モバイルナビ／パンくずは直近監査で健全と確認済み（本タスクで
  破損リンク・空ナビ先の新規発生なし）。

## 13. すでに存在する独自価値シグナル

- 実務的な日本語解説・手順・チェックリスト・比較・州別の区別・よくある失敗の記述。
- インタラクティブツール **14 件**（88 日カウンター・予算計算・ファーム証拠管理など）と
  ダウンロード資料（レジュメ雛形・内見チェックリスト等）。
- YMYL 記事の `verifiedAt`＋`OfficialSourceBox`（公式一次情報リンク・免責）。
- ワーキングホリデー特有の文脈（渡航前〜帰国までのジャーニー）に沿った情報設計（21 ハブ）。

## 14. 手動対応が必要な独自価値の改善

- **Manual**: 主要公開記事に「即答（直接の結論）」「想定読者」「実手順」「必要書類」
  「費用・タイミング（検証済みのもの）」「よくある失敗」「州別の違い」を、公式ソース
  照合のうえで追記・確認する（事実の一括改変は検証なしに行わない）。
- **Manual**: 実在する体験ベース情報を「体験（一般的傾向）」として明示し、公式事実と区別する
  表現の点検（創作は禁止）。
- **Manual**: オリジナル写真・スクリーンショットの追加（既存があれば優先活用、なければ作成）。

## 15. Google Analytics 実装場所

- 設定の集中管理: [lib/siteConfig.ts](lib/siteConfig.ts)（`gaMeasurementId`）。
- コンポーネント: [components/analytics/GoogleAnalytics.tsx](components/analytics/GoogleAnalytics.tsx)
  ＋ [components/analytics/RouteChangeTracker.tsx](components/analytics/RouteChangeTracker.tsx)。
- グローバル読み込み: [app/layout.tsx](app/layout.tsx)（`<body>` 先頭で 1 回だけレンダリング）。
- 型定義: [types/analytics.d.ts](types/analytics.d.ts)（`window.gtag` / `window.dataLayer`）。

## 16. 使用した Measurement ID

- **`G-0SQ2F5Q39G`**（`NEXT_PUBLIC_GA_MEASUREMENT_ID` で上書き可能。空文字で無効化）。
- Measurement ID は**秘密情報ではない**ため、クライアントバンドルに含めて問題ない。

## 17. ページビューの挙動

- `gtag('config', ID, { send_page_view: false })` により**自動初回ページビューを無効化**。
- ページビューの唯一の送信元は `RouteChangeTracker`（`usePathname`＋`useSearchParams`）で、
  **初回ロードとクライアント側ルート遷移（SPA ナビゲーション）の両方**を単一経路で送信し、
  **重複ページビューを防止**。`<Suspense>` 境界内で実行。
- **本番ビルドのみ有効**（`process.env.NODE_ENV === "production"`）。開発・テスト
  （`NODE_ENV="test"`）では読み込まれない。
- `next/script`（`afterInteractive`）で読み込み、**1 回だけ**ロード。ブラウザ拡張等で
  スクリプトがブロックされても `window.gtag` が未定義になるだけで描画は壊れない。
- 開発時の二重初期化に対する軽量ガード（モジュールレベルのフラグ＋警告）を実装。
- **Google Tag Manager は使用していない**（既存でも未使用・不要）。
  **Universal Analytics も追加していない**。**AdSense 広告コードも本タスクでは追加していない**。

## 18. プライバシーポリシーの変更点

- Google Analytics 4（提供元 Google LLC）の利用を明記し、収集する利用情報（閲覧ページ・
  参照元・滞在時間・おおよその地域・端末/ブラウザ種別）、目的（利用状況の把握と改善）、
  Cookie/識別技術の使用、ユーザーのオプトアウト手段（ブラウザ設定・GA オプトアウトアドオン）
  を追加。
- **広告は現時点で未配信**であることを明記し、旧記述（AdSense を配信中のように記載）を是正。
- 将来広告を配信する際は、広告 Cookie・パーソナライズを改めて開示し、必要地域では同意管理を
  導入する旨を追記。
- 最終更新日を **2026 年 8 月 2 日** に更新。

## 19. 同意管理のフォローアップ（Manual・広告配信前の必須作業）

- **Manual**: 広告を配信する前に、適切な同意管理ソリューション（Consent Management Platform）を
  設定する。
- **Manual**: EEA・英国・スイスの訪問者向けに、Google AdSense の「プライバシーとメッセージ」
  または他の Google 認定 CMP を構成する。
- **Manual**: 広告 Cookie・広告のパーソナライズを実際に有効化した時点で、プライバシー
  ポリシーを更新する。
- 本タスクでは、**虚偽のコンプライアンスを主張する独自の広告同意システムは作成していない**。

## 20. 検証結果

| 検証 | 結果 |
| --- | --- |
| `validate:content`（重複 slug/path・dangling・カニバリ） | **0 error / 66 warning**（warning は粗い intent の過検出・dangling 0） |
| `validate:articles`（重複 slug/path/export） | **OK: no article data errors** |
| `test:content`（node --test） | **fail 0** |
| `test:tools`（node --test） | **fail 0** |
| `tsc --noEmit`（型チェック） | **exit 0** |
| `lint`（eslint） | **0 problems**（不要な eslint-disable ディレクティブ 1 件を除去して解消） |
| robots / canonical / sitemap / 構造化データ | 公開フィルタ駆動で未公開を露出せず。`/search` は noindex＋sitemap 除去 |
| Google タグ重複 / Analytics 初期化 | 単一ロード・`send_page_view:false`・重複ページビュー防止を確認 |
| 未公開コンテンツ露出 | draft/review/merged/archived は一覧・sitemap・関連記事から除外（既存仕様） |

## 21. 本番ビルド結果

- **`✓ Compiled successfully`**・**440/440 静的ページ生成**・`prebuild`（`validate:content`）
  0 error。`/editorial-policy` を新規ルートとして生成、`/search` は静的生成のうえ noindex。

## 22. AdSense 再申請前に必要な手動チェック

- **Manual**: 運営者・編集責任の追加情報（可能であれば連絡先メール等）の掲載可否を検討
  （**リポジトリに存在しない運営者情報は創作しない**）。
- **Manual**: 主要公開記事の実務的な深掘り（即答・手順・費用・州別差・よくある失敗）を
  公式ソース照合のうえで強化。
- **Manual**: オリジナル写真／スクリーンショットの追加。
- **Manual**: 掲示板（UGC）の内容が薄い場合の noindex 方針の検討。
- **Manual**: GA4 のリアルタイム計測が本番で発火することを、デプロイ後にブラウザで確認。
- **Manual**: 広告配信を開始する前に同意管理（CMP）を構成（第 19 章参照）。

## 23. 残存リスク

- AdSense の判定ロジックは非公開のため、上記修正で承認される保証はない（**Manual review**）。
- 記事の一部に「公式情報の言い換え中心」に見え得る箇所が残る可能性（**Medium**・手動深掘りで低減）。
- UGC（掲示板）の将来的な薄利化（**Low**・継続監視）。
- `validate:content` の 66 警告は過検出だが、意図クラスタの粒度改善は将来対応（**Low**）。

## 24. 推奨する再申請チェックリスト

1. 本コミットをデプロイし、本番でサイトが正しく表示されることを確認する。
2. 本番で GA4 のリアルタイム計測が発火することをブラウザで確認する。
3. 主要ハブ記事を数本、実務的な深掘り（即答・手順・費用・州別）で強化する（Manual）。
4. 運営者情報・編集方針・プライバシー・お問い合わせへの導線が全ページから到達できることを確認。
5. `/search` などの薄い/内部ページがインデックスされていないことを Search Console で確認。
6. 広告を配信する場合のみ、事前に同意管理（CMP）を構成する。
7. 上記が整ったうえで、**手動で** AdSense 再審査を申請する（本タスクでは申請しない）。

---

### 分類凡例
- **Critical**: 直ちに承認を妨げる重大な問題（本タスクで検出された Critical は無し）。
- **High**: 承認可能性に強く影響（`/search` インデックス・編集方針欠如 → いずれも修正済）。
- **Medium**: 品質・透明性に影響（プライバシー不整合 → 修正済／記事の深掘り → Manual）。
- **Low**: 監視対象（UGC 薄利化・過検出警告）。
- **Manual review required**: 事実確認・運営者情報・写真・同意管理・審査申請。
